import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ghanacedicounter.app',
  appName: 'Ghana Cedi Counter',
  webDir: 'build'
};

export default config;
